import type { NextPage } from "next";

import MainPage from './mainPage';
import Header from '@components/header';

const Home: NextPage = () => {

  return (
    <section>
      <MainPage />
    </section>
  );
  
}

export default Home;
